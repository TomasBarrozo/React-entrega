import { createTheme } from "@mui/material";

export const customTheme = createTheme({
  palette: {
    primary: {
      main: "#d87d4a",
      secondary: "#fbaf85",
    },
    secondary: {
      main: "#f1f1f1",
      secondary: "#fafafa",
    },
  },
});
